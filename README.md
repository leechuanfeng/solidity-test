# Simple Solidity Contract Test

This is a simple solidity test project.

## Setup

Make sure to install the dependencies

```bash
yarn install
```

## Run the project

Run the following command to execute the test

```bash
npx hardhat compile
npx hardhat test
```